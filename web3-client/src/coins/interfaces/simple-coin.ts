

export interface SimpleCoin {
    id: string;
    image: string;
    symbol: string;
    name: string;
    current_price: number;
    total_volume: number;
    market_cap_change_percentage_24h: number;
}